HUNAR MOBILE-ONLY RESPONSIVE UPDATE

This package changes the frontend responsive/mobile CSS only.
It does NOT intentionally change Supabase, authentication, OTP, Google login, contracts, payments, Chapa, wallet logic, messaging logic, admin logic, APIs, Edge Functions, security/RLS, or business logic.

Replace the existing index.html with this index.html in the existing HUNAR website.
Keep all existing supporting files (including hunar-services.js and hunar-safety.js) unchanged.

Mobile targets:
320 / 360 / 375 / 390 / 414 / 430px, tablet, portrait and landscape.

After publishing, hard refresh the site on the phone/browser.
